apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wf_manager.fullname" . }}-test-connection"
  labels:
    {{- include "wf_manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wf_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
